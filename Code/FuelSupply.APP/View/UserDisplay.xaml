<UserControl x:Class="FuelSupply.APP.View.UserDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Name="txtTitle" Text="User List" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center"></TextBlock>
        <Grid Grid.Column="0" Grid.Row="2" Background="White">
            <DataGrid ItemsSource="{Binding UserList}" AutoGenerateColumns="False" >
                
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
